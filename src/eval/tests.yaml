- vars:
    id: bird-89
    query: How many accounts who choose issuance after transaction are staying in
      East Bohemia region?
    expected_sql: SELECT COUNT(T2.account_id) FROM district AS T1 INNER JOIN account
      AS T2 ON T1.district_id = T2.district_id WHERE T1.A3 = 'east Bohemia' AND T2.frequency
      = 'POPLATEK PO OBRATU'
    reference_answer: There are 13 accounts that issued transactions after their last
      transaction took place in the East Bohemia region.
    difficulty: moderate
- vars:
    id: bird-92
    query: List out the no. of districts that have female average salary is more than
      6000 but less than 10000?
    expected_sql: SELECT COUNT(DISTINCT T2.district_id)  FROM client AS T1 INNER JOIN
      district AS T2 ON T1.district_id = T2.district_id WHERE T1.gender = 'F' AND
      T2.A11 BETWEEN 6000 AND 10000
    reference_answer: There are 69 districts where the average female salary is between
      6,000 and 10,000.
    difficulty: simple
- vars:
    id: bird-93
    query: How many male customers who are living in North Bohemia have average salary
      greater than 8000?
    expected_sql: SELECT COUNT(T1.client_id) FROM client AS T1 INNER JOIN district
      AS T2 ON T1.district_id = T2.district_id WHERE T1.gender = 'M' AND T2.A3 = 'north
      Bohemia' AND T2.A11 > 8000
    reference_answer: There are 280 male customers living in North Bohemia with an
      average salary greater than 8,000.
    difficulty: moderate
- vars:
    id: bird-94
    query: List out the account numbers of female clients who are oldest and has lowest
      average salary, calculate the gap between this lowest average salary with the
      highest average salary?
    expected_sql: SELECT T1.account_id , ( SELECT MAX(A11) - MIN(A11) FROM district
      ) FROM account AS T1 INNER JOIN district AS T2 ON T1.district_id = T2.district_id
      INNER JOIN disp AS T3 ON T1.account_id = T3.account_id INNER JOIN client AS
      T4 ON T3.client_id = T4.client_id WHERE T2.district_id = ( SELECT district_id
      FROM client WHERE gender = 'F' ORDER BY birth_date ASC LIMIT 1 ) ORDER BY T2.A11
      DESC LIMIT 1
    reference_answer: The account number of the female client who is oldest and has
      the lowest average salary is 6. The gap between this lowest average salary and
      the highest average salary is 4431.
    difficulty: challenging
- vars:
    id: bird-95
    query: List out the account numbers of clients who are youngest and have highest
      average salary?
    expected_sql: SELECT T1.account_id  FROM account AS T1 INNER JOIN disp AS T2 ON
      T1.account_id = T2.account_id INNER JOIN client AS T3 ON T2.client_id = T3.client_id
      INNER JOIN district AS T4 on T4.district_id = T1.district_id WHERE T2.client_id
      = ( SELECT client_id FROM client ORDER BY birth_date DESC LIMIT 1) GROUP BY
      T4.A11, T1.account_id
    reference_answer: The client with the youngest age has an account number of 2836.
    difficulty: moderate
- vars:
    id: bird-98
    query: Among the accounts who have approved loan date in 1997, list out the accounts
      that have the lowest approved amount and choose weekly issuance statement.
    expected_sql: SELECT T2.account_id FROM loan AS T1 INNER JOIN account AS T2 ON
      T1.account_id = T2.account_id WHERE STRFTIME('%Y', T1.date) = '1997' AND T2.frequency
      = 'POPLATEK TYDNE' ORDER BY T1.amount LIMIT 1
    reference_answer: Among the accounts that approved a loan in 1997, account 176
      has the lowest approved amount and selects the weekly issuance statement.
    difficulty: moderate
- vars:
    id: bird-99
    query: Among the accounts who have loan validity more than 12 months, list out
      the accounts that have the highest approved amount and have account opening
      date in 1993.
    expected_sql: SELECT T1.account_id FROM loan AS T1 INNER JOIN account AS T2 ON
      T1.account_id = T2.account_id WHERE STRFTIME('%Y', T2.date) = '1993' AND T1.duration
      > 12 ORDER BY T1.amount DESC LIMIT 1
    reference_answer: The account with ID 10451 has the highest approved amount among
      accounts with loan validity greater than 12 months and an account opening date
      in 1993.
    difficulty: moderate
- vars:
    id: bird-100
    query: Among the account opened, how many female customers who were born before
      1950 and stayed in Sokolov?
    expected_sql: SELECT COUNT(T2.client_id) FROM district AS T1 INNER JOIN client
      AS T2 ON T1.district_id = T2.district_id WHERE T2.gender = 'F' AND STRFTIME('%Y',
      T2.birth_date) < '1950' AND T1.A2 = 'Sokolov'
    reference_answer: Among the accounts opened, there are 8 female customers born
      before 1950 who stayed in Sokolov.
    difficulty: moderate
- vars:
    id: bird-112
    query: For the female client who was born in 1976/1/29, which district did she
      opened her account?
    expected_sql: SELECT T1.A2 FROM district AS T1 INNER JOIN client AS T2 ON T1.district_id
      = T2.district_id WHERE T2.birth_date = '1976-01-29' AND T2.gender = 'F'
    reference_answer: The female client born on January 29, 1976, opened her account
      in the Tachov district.
    difficulty: simple
- vars:
    id: bird-115
    query: For the branch which located in the south Bohemia with biggest number of
      inhabitants, what is the percentage of the male clients?
    expected_sql: SELECT CAST(SUM(T1.gender = 'M') AS REAL) * 100 / COUNT(T1.client_id)
      FROM client AS T1 INNER JOIN district AS T2 ON T1.district_id = T2.district_id
      WHERE T2.A3 = 'south Bohemia' GROUP BY T2.A4 ORDER BY T2.A4 DESC LIMIT 1
    reference_answer: The branch in south Bohemia with the largest number of inhabitants
      has a male client percentage of approximately 44.26%.
    difficulty: challenging
- vars:
    id: bird-116
    query: For the client whose loan was approved first in 1993/7/5, what is the increase
      rate of his/her account balance from 1993/3/22 to 1998/12/27?
    expected_sql: SELECT CAST((SUM(CASE WHEN T3.date = '1998-12-27' THEN T3.balance
      ELSE 0 END) - SUM(CASE WHEN T3.date = '1993-03-22' THEN T3.balance ELSE 0 END))
      AS REAL) * 100 / SUM(CASE WHEN T3.date = '1993-03-22' THEN T3.balance ELSE 0
      END) FROM loan AS T1 INNER JOIN account AS T2 ON T1.account_id = T2.account_id
      INNER JOIN trans AS T3 ON T3.account_id = T2.account_id WHERE T1.date = '1993-07-05'
    reference_answer: The account balance increased by approximately 430.45% from
      March 22, 1993, to December 27, 1998, for the client whose loan was approved
      first on July 5, 1993.
    difficulty: challenging
- vars:
    id: bird-117
    query: What is the percentage of loan amount that has been fully paid with no
      issue.
    expected_sql: SELECT (CAST(SUM(CASE WHEN status = 'A' THEN amount ELSE 0 END)
      AS REAL) * 100) / SUM(amount) FROM loan
    reference_answer: The percentage of loan amount that has been fully paid with
      no issue is approximately 18.02%.
    difficulty: moderate
- vars:
    id: bird-118
    query: For loan amount less than USD100,000, what is the percentage of accounts
      that is still running with no issue.
    expected_sql: SELECT CAST(SUM(status = 'C') AS REAL) * 100 / COUNT(account_id)
      FROM loan WHERE amount < 100000
    reference_answer: For loan amounts under USD100,000, approximately 46.89% of accounts
      are still running without any issues.
    difficulty: moderate
- vars:
    id: bird-125
    query: For loans contracts which are still running where client are in debt, list
      the district of the and the state the percentage unemployment rate increment
      from year 1995 to 1996.
    expected_sql: SELECT CAST((T3.A13 - T3.A12) AS REAL) * 100 / T3.A12 FROM loan
      AS T1 INNER JOIN account AS T2 ON T1.account_id = T2.account_id INNER JOIN district
      AS T3 ON T2.district_id = T3.district_id WHERE T1.status = 'D'
    reference_answer: The percentage unemployment rate increment from 1995 to 1996
      for loans contracts where clients are still in debt ranges from 8.44% to 115%.
    difficulty: challenging
- vars:
    id: bird-128
    query: List the top nine districts, by descending order, from the highest to the
      lowest, the number of female account holders.
    expected_sql: SELECT T2.A2, COUNT(T1.client_id) FROM client AS T1 INNER JOIN district
      AS T2 ON T1.district_id = T2.district_id WHERE T1.gender = 'F' GROUP BY T2.district_id,
      T2.A2 ORDER BY COUNT(T1.client_id) DESC LIMIT 9
    reference_answer: The top nine districts by the number of female account holders,
      from highest to lowest, are Hl.m. Praha (324), Karvina (88), Ostrava - mesto
      (84), Brno - mesto (75), Zlin (57), Olomouc (50), Frydek - Mistek (42), Kolin
      (39), and Nachod (37).
    difficulty: moderate
- vars:
    id: bird-136
    query: Between 1/1/1995 and 12/31/1997, how many loans in the amount of at least
      250,000 per account that chose monthly statement issuance were approved?
    expected_sql: SELECT COUNT(T1.account_id) FROM account AS T1 INNER JOIN loan AS
      T2 ON T1.account_id = T2.account_id WHERE T2.date BETWEEN '1995-01-01' AND '1997-12-31'
      AND T1.frequency = 'POPLATEK MESICNE' AND T2.amount >= 250000
    reference_answer: Between January 1, 1995, and December 31, 1997, there were 65
      loans with an amount of at least $250,000 per account and a choice for monthly
      statement issuance that were approved.
    difficulty: moderate
- vars:
    id: bird-137
    query: How many accounts have running contracts in Branch location 1?
    expected_sql: SELECT COUNT(T1.account_id) FROM account AS T1 INNER JOIN district
      AS T2 ON T1.district_id = T2.district_id INNER JOIN loan AS T3 ON T1.account_id
      = T3.account_id WHERE T1.district_id = 1 AND (T3.status = 'C' OR T3.status =
      'D')
    reference_answer: There are 47 accounts with running contracts in Branch location
      1.
    difficulty: moderate
- vars:
    id: bird-145
    query: Who are the account holder identification numbers whose who have transactions
      on the credit card with the amount is less than the average, in 1998?
    expected_sql: SELECT T1.account_id FROM trans AS T1 INNER JOIN account AS T2 ON
      T1.account_id = T2.account_id WHERE STRFTIME('%Y', T1.date) = '1998' AND T1.operation
      = 'VYBER KARTOU' AND T1.amount < (SELECT AVG(amount) FROM trans WHERE STRFTIME('%Y',
      date) = '1998')
    reference_answer: The account identification numbers with transactions on the
      credit card in 1998 that are less than the average amount include 14, 33, 34,
      43, 48, 51, 65, 66, 68, 71, 73, 95, 96, 97, 106, 108, 109, and 110.
    difficulty: moderate
- vars:
    id: bird-149
    query: Please list the account types that are not eligible for loans, and the
      average income of residents in the district where the account is located exceeds
      $8000 but is no more than $9000.
    expected_sql: SELECT T3.type FROM district AS T1 INNER JOIN account AS T2 ON T1.district_id
      = T2.district_id INNER JOIN disp AS T3 ON T2.account_id = T3.account_id WHERE
      T3.type != 'OWNER' AND T1.A11 BETWEEN 8000 AND 9000
    reference_answer: The account types that are not eligible for loans and the average
      income of residents in the district where the account is located exceeds $8000
      but is no more than $9000 are Dispensor.
    difficulty: challenging
- vars:
    id: bird-152
    query: What is the average number of crimes committed in 1995 in regions where
      the number exceeds 4000 and the region has accounts that are opened starting
      from the year 1997?
    expected_sql: SELECT AVG(T1.A15) FROM district AS T1 INNER JOIN account AS T2
      ON T1.district_id = T2.district_id WHERE STRFTIME('%Y', T2.date) >= '1997' AND
      T1.A15 > 4000
    reference_answer: The average number of crimes committed in 1995 for regions with
      more than 4,000 crimes and where account openings started after 1997 is approximately
      29,670.45.
    difficulty: moderate
- vars:
    id: bird-159
    query: List all the withdrawals in cash transactions that the client with the
      id 3356 makes.
    expected_sql: SELECT T4.trans_id FROM client AS T1 INNER JOIN disp AS T2 ON T1.client_id
      = T2.client_id INNER JOIN account AS T3 ON T2.account_id = T3.account_id INNER
      JOIN trans AS T4 ON T3.account_id = T4.account_id WHERE T1.client_id = 3356
      AND T4.operation = 'VYBER'
    reference_answer: The client with ID 3356 made cash withdrawal transactions with
      IDs 816173 through 816218.
    difficulty: simple
- vars:
    id: bird-168
    query: What percentage of clients who opened their accounts in the district with
      an average salary of over 10000 are women?
    expected_sql: SELECT CAST(SUM(T2.gender = 'F') AS REAL) * 100 / COUNT(T2.client_id)
      FROM district AS T1 INNER JOIN client AS T2 ON T1.district_id = T2.district_id
      WHERE T1.A11 > 10000
    reference_answer: Based on the query results, 49.61% of clients who opened their
      accounts in a district with an average salary over 10,000 are women.
    difficulty: moderate
- vars:
    id: bird-169
    query: What was the growth rate of the total amount of loans across all accounts
      for a male client between 1996 and 1997?
    expected_sql: SELECT CAST((SUM(CASE WHEN STRFTIME('%Y', T1.date) = '1997' THEN
      T1.amount ELSE 0 END) - SUM(CASE WHEN STRFTIME('%Y', T1.date) = '1996' THEN
      T1.amount ELSE 0 END)) AS REAL) * 100 / SUM(CASE WHEN STRFTIME('%Y', T1.date)
      = '1996' THEN T1.amount ELSE 0 END) FROM loan AS T1 INNER JOIN account AS T2
      ON T1.account_id = T2.account_id INNER JOIN disp AS T3 ON T3.account_id = T2.account_id
      INNER JOIN client AS T4 ON T4.client_id = T3.client_id WHERE T4.gender = 'M'
      AND T3.type = 'OWNER'
    reference_answer: The growth rate of the total amount of loans across all accounts
      for a male client between 1996 and 1997 was 25.3%.
    difficulty: challenging
- vars:
    id: bird-186
    query: What percentage of male clients request for weekly statements to be issued?
    expected_sql: SELECT CAST(SUM(T1.gender = 'M') AS REAL) * 100 / COUNT(T1.client_id)
      FROM client AS T1 INNER JOIN district AS T3 ON T1.district_id = T3.district_id
      INNER JOIN account AS T2 ON T2.district_id = T3.district_id INNER JOIN disp
      as T4 on T1.client_id = T4.client_id AND T2.account_id = T4.account_id WHERE
      T2.frequency = 'POPLATEK TYDNE'
    reference_answer: 52.63% of the male clients request for weekly statements to
      be issued.
    difficulty: moderate
- vars:
    id: bird-189
    query: Name the account numbers of female clients who are oldest and have lowest
      average salary?
    expected_sql: SELECT T3.account_id FROM client AS T1 INNER JOIN district AS T2
      ON T1.district_id = T2.district_id INNER JOIN account AS T3 ON T2.district_id
      = T3.district_id INNER JOIN disp AS T4 ON T1.client_id = T4.client_id AND T4.account_id
      = T3.account_id  WHERE T1.gender = 'F' ORDER BY T1.birth_date ASC, T2.A11 ASC
      LIMIT 1
    reference_answer: The account number for the oldest female client with the lowest
      average salary is 1743.
    difficulty: moderate
- vars:
    id: bird-192
    query: What is the average amount of loan which are still on running contract
      with statement issuance after each transaction?
    expected_sql: SELECT AVG(T2.amount) FROM account AS T1 INNER JOIN loan AS T2 ON
      T1.account_id = T2.account_id WHERE T2.status IN ('C', 'D') AND T1.frequency
      = 'POPLATEK PO OBRATU'
    reference_answer: The average loan amount for transactions with ongoing contracts
      and statement issuances is $192,836.57.
    difficulty: moderate
- vars:
    id: bird-119
    query: For accounts in 1993 with statement issued after transaction, list the
      account ID, district name and district region.
    expected_sql: SELECT T1.account_id, T2.A2, T2.A3 FROM account AS T1 INNER JOIN
      district AS T2 ON T1.district_id = T2.district_id WHERE T1.frequency = 'POPLATEK
      PO OBRATU' AND STRFTIME('%Y', T1.date)= '1993'
    reference_answer: 'The accounts with statement issued after transaction in 1993
      are: account ID 66 (Rychnov nad Kneznou - east Bohemia), 273 (Karlovy Vary -
      west Bohemia), 485 (Kutna Hora - central Bohemia), 539 (Rakovnik - central Bohemia),
      1050 (Hodonin - south Moravia), 1286 (Zdar nad Sazavou - south Moravia), 1527
      (Tachov - west Bohemia), 1539 (Hl.m. Praha - Prague), 1965 (Hodonin - south
      Moravia), 2137 (Kladno - central Bohemia), 2464 (Decin - north Bohemia), 2705
      (Vyskov - south Moravia), 3837 (Rychnov nad Kneznou - east Bohemia), 4013 (Opava
      - north Moravia), 5228 (Vsetin - north Moravia), 5256 (Olomouc - north Moravia),
      7123 (Zlin - south Moravia), 7774 (Ostrava - mesto - north Moravia), and 8073
      (Karlovy Vary - west Bohemia).'
    difficulty: moderate
- vars:
    id: bird-120
    query: From Year 1995 to 2000, who are the accounts holders from 'east Bohemia'.
      State the account ID the frequency of statement issuance.
    expected_sql: SELECT T1.account_id, T1.frequency FROM account AS T1 INNER JOIN
      district AS T2 ON T1.district_id = T2.district_id WHERE T2.A3 = 'east Bohemia'
      AND STRFTIME('%Y', T1.date) BETWEEN '1995' AND '2000'
    reference_answer: From 1995 to 2000, the account holders from east Bohemia include
      accounts with IDs 14, 76, 80, 84, 103, 131, 149, 171, 174, 182, 194, 210, 232,
      253, 283, 292, 296, and 311. The frequency of statement issuance for these accounts
      is mostly 'POPLATEK MESICNE'.
    difficulty: moderate
