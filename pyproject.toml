[project]
name = "spx-automation-technical"
version = "0.1.0"
description = "A local supermarket query dashboard with TinyFish ETL and deterministic SQL generation."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
